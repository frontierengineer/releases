# Frontier 0.1.0-nightly.20260616

- docs: application rename + public-infra follow-on complete (remove stale HANDOFF, correct APPLICATIONS kept/done lists)
- Daemon wire: /extensions/* → /applications/* worker URLs + ext.msg → application.msg envelope (extension field → application)
- Host: application.json manifest filename + DEFAULT_REGISTRY → frontierengineer/applications
- Fix the dumbass job that was done earlier
- demo.yaml: plain container (intrinsic demo mode, no k8s scaffolding) + demo-k8s worker
- HANDOFF: merge apps-rename + deploy/verify + public-infra follow-on (for a fresh session)
- applicationDevMcp: rename the dev MCP tool names extensions_* -> applications_*
- docs: unify the concept term to "application"; consolidate APPLICATIONS.md
- Rename the extension/app concept to a single term: "application"
- Per-machine diagnostics: daemon reports CPU load/cores, memory, GPU (nvidia-smi best-effort) on heartbeat; surfaced via machines.list
- Apps-model shell polish: stripes, top bar home, resources, machines tree, dictation
