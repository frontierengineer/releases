# Frontier 0.1.0-nightly.20260614

- Extensions table: shrink capability-column headers to 10px
- Extensions: exclude root config.json from managed-file enumeration
- Extensions: emit extension.reloaded from enable/disable handlers
- Extensions: drop run-location pill from marketplace cards, add per-row enable toggle
- Extensions table: share one set of column tracks (fix header/row misalignment)
- Frontend: activity bar SWITCHES view containers; single-view side bar; watermark empty editor
- Extensions: container icons + create-command groups; remove all welcome-tile contributions
- Sidebar contract + store: view-container model, sidebar icons, command groups, drop welcome-tile API
- Frontier: Welcome core extension — uninstallable first-run anchor
- Frontier: revive the dead empty pane as a milestone-gated first-run welcome
- Frontier shell: section headers go accent-on-active-only (calm)
- Frontier shell: arrangeable sidebar groups — stacked|tabbed by choice, per panel
- Frontier shell: persistent VS Code-style activity bar + discoverable command palette
- Frontier UI: adopt tri-state dot + grouping in assistant/chat/meetings/recordings
- Frontier UI: tri-state presence dot + shared grouped sidebar-list primitives
- Chat: honest turn states + activity watchdog so a turn never spins forever
- Chat first turn: don't let the worker SIGTERM a fresh git worktree mid-checkout
- docs/reviews: Playwright onboarding + UX audits of the master UI (advisory)
- docs(workspaces): a vanished worktree is released + owner-notified, never silently rebuilt
- Workspaces: release (never self-heal) a vanished slot, and verify worktree creation on disk
- meetings: completing a recording is final; move Add note into the transcript
- meetings: trim live-summary header, make a completed summary static
- frontend: serve PWA manifest with a channel-tinted theme_color
- Workspaces: keep one warm spare slot ready ahead of demand (prewarm)
- Workspaces: self-heal a vanished slot dir on dispatch (ensureSlot)
- Frontier left sidebar: absolute section sizes (VS Code), drop ratios model
- Frontier sidebar: add VS Code SplitView size-transfer helpers
- Frontier frontend: per-pane scroll containers (VS Code model), drop blunt #root * overscroll
- Skills MCP: let agents create/update/list skills, not just search
- ui: rename the Sessions nav destination (was Workspaces); drop the personas preamble blurb
- docs(machines): all three connect phases built + live — relay incl. MCP, bare connect, indicator, connect.sh
- release: stable cut refreshes the public connect/daemon.bundle.js (keeps the native bare-code path current)
- protocol: connectionMode on MachineStatus (frontend reads it for the direct/via-relay indicator)
- machines: expose connectionMode (direct | relay | undefined) for the UI
- worker transport: bare `connect <code>` resolves the host via Link
- worker transport: bridge MCP over the control WS so relayed agent turns reach their tools
- Frontier Machines: show direct vs via-relay transport on connected rows
- Frontier extensions: self-heal removed built-ins (quarantine ghosts at boot)
