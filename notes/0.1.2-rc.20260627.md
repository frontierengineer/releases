# Frontier 0.1.2-rc.20260627

- app: terse package.json description (the verbose one ships in the .dmg)
- release: bump to 0.1.2; rewrite the cut gotchas in plain English
- machines: wire custom-port for real (no stub) + make Local/Remote modes coherent
- release: link container package to releases repo; strip internal metadata from public artifacts
- settings: unify MCP/Agents/Skills/Personas empty states; fix About release info; custom-port banner
- palette: dedup Meetings/Spaces create rows (boot-order-resilient); New Meeting opens its meeting
- machines: redesign Connect-a-machine as a 5-stage accordion + auto Docker provision
- machines: machine.docker.provision (auto-run Docker sandboxes) + Docker capability probe
- machines: render Docker as a Capability row
- machines view v2: expand-all default, 5s slot poll, full-width detail, tooltips, terminals, workspace de-table
- app/welcome: Add-Host redesign — Recent-first, SSH/Direct tabs, save-to-ssh-config
- extensions: editor Close returns to list; update button (confirm + progress) in the ... menu
- extensions: build/run/update re-applies ALL capabilities; drop spurious no-UI warning
- sessions: machine fallback via reservation; chat Stop shows 'Stopped' not failure
- docs: release preflight & gotchas (public image, tag-push, version files)
- palette: one section per extension; merge create commands; redirect + no-echo + title fit
- machines/telemetry: worker toolchains probe (Capabilities) + default memory MCP
- Docs change
- sessions: clarify session/reservation/machine UX; fix chat deep-link + Stop button
- docs/security: align data-secrets to the simplified proof-of-origin design
- telemetry: simplify attestation to one benign install event; drop from extensions
- machines: VS Code-style tree+detail redesign; connection types; Capabilities
- app/welcome: restructure connect modal into Recent / SSH / SSH hosts / Direct
- telemetry: obfuscate the shipped attestation path
- fix: chat Stop button deadlocked on ensureSession — use the live session
- machines: core terminal in the Machines view (core PTY, no extension dependency)
- fix: drop accidental corePty import swept into index.ts, unbreaking the build
- feat: Session.cancel + in-chat Stop button; surface marketplace install errors
- fix: workspace-picker crash (no-title workspace) + NFS git-checkout deadline
- QA: harden agentic extensions + opencode "no login needed" state
- machines: split runtimes/workspaces into titled sections; tidy runtime columns; drop terminal reliance
- core: extract terminal to the marketplace (drop the built-in + the core dependency)
