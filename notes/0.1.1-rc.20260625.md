# Frontier 0.1.1-rc.20260625

- release: bump VERSION to 0.1.1
- release: app owns bare channel tags, host bundle moves to host-*
- feedback: wrap long diagnostics values instead of clipping horizontally
- chrome: deterministic left activity-bar order (stable id tiebreak)
- chrome: move explanatory tooltips to the Info View + palette leading-edge selection (task 12)
- guide: graceful turn-error/stall state + Retry (never appear hung)
- security-docs: apply review fixes (re-pin bundler.ts citations, reframe eval opt-in as wired, drop unverified per-IP pairing, +voice-sidecar/CSWSH/Electron-isolation coverage)
- layout: app min-width + horizontal scroll when sidebars active; fix dictation menu left-edge overflow
- batch: host-unreachable UX, switch-host/SSH, guide, games, feedback, security docs
- deploy: give each env its own /frontier/home (un-share the root)
- host: make the Docker image look like a normal Frontier host
- core: platform + dev-loop fixes found during the extension QA pass
- QA hardening: fix ~34 bugs across 13 extensions (live-driven against master)
- app: wire auto-update feed + publish the artifacts manifests reference
- app: fix Windows installer build (NSIS chokes on $HOME in description)
- app release: ship the bundled backend from CI, add x64 + Windows
- Small css tweaks
- Again
- shader clean
- guide: remove dead appearance.ts
- guide: run the presence shader verbatim (WebGL2 + real iChannel0), strip the scaffolding
- guide: update presence visualizer to the new ring shader; react to the guide's voice only
- guide/sidebar review fixes: disconnect TTS source node, resize over-subscribed guard, two-arg atan
- Rename onboarding->guide; status-bar chips right + new resize model; audio-reactive guide shader
- onboarding: refine the presence orb's flow-field shader
- Sidebar system S2: chip badge/attention API, full-surface overlay, disabled-removal cleanup; migrate alerts + onboarding off legacy status items
- Sidebar system: standalone side-by-side sidebars driven by draggable status-bar chips
- Review fixes: capture race, host-identity gate, builtin prune, cursor robustness, regex char-class
- cursor runtime: new provider wrapping the Cursor headless CLI (cursor-agent)
- UI/UX batch: topbar metrics, onboarding (Guide title + focus-driven voice + reset + flow shader), screen-capture source picker, known-hosts switcher, friendly markdown-replacement placeholders, drop removed-builtins quarantine
- App build changes
- onboarding: store the "met the guide" flag in services.store, not localStorage
- onboarding: soft barge-in — pause on detect, commit if sustained, resume if it fizzles
- onboarding: replace the scripted step-machine with an open, goal-driven conversation
- onboarding: simplify the guide's speech to a dead-simple sequential player
- onboarding: speak through the engine (sherpa), disable the native macOS voice
- onboarding: don't poison the next TTS utterance on barge-in (cancel->speak quirk)
- onboarding: stop the guide cutting off its own voice (mic echo barge-in)
- onboarding: living realtime presence + fix the core onSpeak wire
- screen share: snapshot-on-confirmation capture of the user's screen for the assistant
- sessions: correct a misleading comment on the sidebar-route close
- sessions: open a sidebar-owned session's rail panel, not a shared app
- onboarding: authored walkthrough + guide status item; generic chrome-control host methods
- address code-review findings across the UI/UX batch
- UI/UX batch: native fullscreen, host pill, editor, transcript, media, voice tests
- Remove stale onboarding-evolution proposal (superseded; redo tracked in ROADMAP)
- Media renders by file extension (no raw HTML) + step-by-step .dmg signing guide
- Follow-ups: alerts→right sidebar, fs rename/delete, media in chat, summarize-now
- App: load electron-updater lazily so a missing module can't crash app launch
- Dev Browser extension: worker-side Playwright for agents (the 'build a web app, then look at it' loop)
