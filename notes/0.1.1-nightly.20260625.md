# Frontier 0.1.1-nightly.20260625

- machines: split runtimes/workspaces into titled sections; tidy runtime columns; drop terminal reliance
- core: extract terminal to the marketplace (drop the built-in + the core dependency)
- opencode: seed a zero-setup default model so first turns don't hang
- app: bump to 0.1.1 (lockstep with host VERSION)
- ci: cut-rc needs contents:write (build-app reusable requires it)
- host-like layout: HOME=/root, data at ~/.frontier (drop symlink)
