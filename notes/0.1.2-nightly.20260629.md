# Frontier 0.1.2-nightly.20260629

- build:host — install the cli's runtime deps before bundling it
- host: sign reachId registrations (re-vendor link-client) + worker signing + sharding config
- desktop: reach the host over the authed library device channel + auto-auth (staged)
- host: ship the `frontier` CLI in the host image + a standalone host installer
- host: fix CI build — exclude the cli package from the backend tsc gate
- host: add a local admin socket + the `frontier` CLI (no-UI pairing/admin)
- cleanup: mark iOS PORT_STATUS verified; remove superseded mobile-selftest
- android: fix unclosed comment in HostInterceptor.kt doc (Kotlin nests block comments)
- android: commit the missing gradle-wrapper.jar (CI ./gradlew was broken)
- mobile: port iOS + Android native apps onto the @frontier/link-client wire
- connection: re-vendor link-client (idle reaper both-ways + dial-back hard ceiling)
- connection: harden the device channel (adversarial-review fast-follows)
- ci: add native mobile (iOS + Android) build+test workflow
- connection: migrate the device /ws channel onto @frontier/link-client
